import React from "react";
import withMargin from "../../common/withMargin";
class Team extends React.Component {
  render() {
    return (
      <div>
        To add pokemon to your team, search for the pokemon you want to add then
        click the 'Team Up!' button [WIP]
      </div>
    );
  }
}

export default withMargin(Team);
