import React from 'react'
import './Loading.css'
import image from '../loading.png'
const Loading = () => {
    return (
        <img className="Loading" src={image} alt="Loading"></img>
    )
}

export default Loading